[pytest]
# v5.2 修复：测试配置（修复计划 v1.2 遗漏，由实现者补充）
# pythonpath=. 将 demo2/ 加入 sys.path，使 tests/ 下测试可 `from frost_agent import ...`
# （frost_agent.py 内部用绝对导入 from models / harness / search 等，均依赖 demo2/ 在 sys.path）
pythonpath = .
# strict 模式：仅 @pytest.mark.asyncio 标记的 async 测试走 asyncio 事件循环
asyncio_mode = strict
