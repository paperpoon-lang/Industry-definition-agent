# Trae\-Project\-Brief

# Trae Project Brief — 行业定义 Agent

> 给 Trae 的项目背景书 \| 2026\-06\-04

---

## 一、这是什么项目

为行业定义（，全球咨询公司）做一个 Demo：**输入行业名称，自动产出客户可读的"行业定义"报告。**

注意——是"行业定义"，不是"完整行研报告"。不含竞争排名、市场规模预测、投资建议。

---

## 二、用户与场景

|维度|说明|
|---|---|
|**终端读者**|行业定义客户（对外交付物）|
|**直接用户**|行业定义分析师——新入职快速上手陌生行业 / 资深分析师新项目第一环节自动化|
|**调用方式**|命令行：`python frost_agent.py "行业名称"` → 输出 Markdown 报告|

---

## 三、为什么不直接让 LLM 随便写

导师说"让 AI 自行决策行业定义怎么写"——但完全自由会导致输出像维基百科第一段，不像行业定义。

我们选择**方案 C：原则驱动**——不是给 Agent 填表模板，也不是让它完全自由。而是把行业定义分析师的判断原则（怎么选维度、怎么画边界、怎么区分结构性和时效性）翻译成 Agent 可执行的推理规则。

详见：`方法论-v2.md`（Agent 的"大脑"）

---

## 四、为什么用这个架构

经过调研和跨 Agent 讨论，选定了 **A2：结构化分步单 Agent \+ State 驱动**。

|不做|要做|
|---|---|
|❌ 一个 prompt 吞进去吐出来（A1：大 Prompt 一口气）|✅ 六步显式分步（A2）|
|❌ 多个 Agent 各自负责一步（方案 C：编排器\+专家）|✅ 同一个 LLM，每一步有明确的输入输出契约|

**选 A2 的原因**：方法论必须贯穿全程，不能在不同步骤间丢失。A2 是方案 C 的退化版本——未来升级到多 Agent，只需把某个 step 的调用换成独立 Agent，State Schema 和 Step Contracts 不变。

---

## 五、已有交付物

|文档|用途|位置|
|---|---|---|
|**方法论\-v2\.md**|Agent 的"大脑"——判断标准、推理规则、自检清单|飞书工作文件夹 / 素材沉淀|
|**架构设计/架构设计\-Agent架构\-v2\.md**|技术 spec（v2）：State Schema、六步 Step Contracts、Harness/Environment 分层、Orchestrator 伪代码|项目仓库|

---

## 六、你要做什么（阶段三）

基于架构设计文档实现 Demo。四个文件：

|文件|内容|
|---|---|
|`frost_agent.py`|主流程：Orchestrator \+ 六步逻辑 \+ CLI 入口|
|`models.py`|State Schema 数据模型（StepOutput, ReportState, StepContext）|
|`methodology_loader.py`|加载方法论文档 \+ 按 step\_id 切片注入|
|`requirements.txt`|pydantic, openai（或 deepseek sdk）, tavily\-python|

---

## 七、关键约束

1. **每一步输出必须包含 ****`reasoning`**** 字段**——不能只有结论，必须有"为什么这样判断"

2. **Confidence 必须标注**——高/中/低 \+ 一句依据

3. **Step 5 自检不通过 → 不允许直接输出**，打印警告并标记 `fixes_required`

4. **搜索至少 3 轮**，覆盖：行业定义 \+ 政策标准 \+ 边界争议

5. **方法论文档作为外部文件加载**，不硬编码在 prompt 里——未来改方法论不用改代码

---

## 八、验收标准

- `python frost_agent.py "低空经济物流"` 能稳定产出报告

- 输出的方法论附注里能看到完整的维度取舍推理

- Step 5 自检能全部 pass

- 报告中不含竞争排名、市场份额、投资建议

---

## 九、项目状态

|阶段|状态|负责人|
|---|---|---|
|阶段一：方法论提炼|✅ 完成|PAPER \+ Hermes|
|阶段二：Agent 架构设计|✅ 初稿完成|PAPER \+ Hermes \+ Kimi|
|**阶段三：Demo 开发**|🔴 **待开工**|**Trae**|
|阶段四：反思与迭代|🔴|PAPER|

