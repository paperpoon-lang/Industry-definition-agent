# 行业定义Agent开发方法论\-沉淀总结\-2026\-08\-13

# 行业定义 Agent 开发方法论——从 11 轮实测迭代中沉淀的设计清单

> 来源：trusted\-retrieval\-builder（可信检索/行业定义 Skill）v2\.1 → v2\.5\.2 的完整设计历程
沉淀日期：2026\-08\-13
每条方法论 = 一个真实翻车案例 \+ 一次设计修正 \+ 至少一次验证。没有凭空的设计，只有踩出来的规则。

---

## 一、总纲：四条贯穿始终的原则

### 1\. 先想失败，再谈设计

每个机制设计前先列「这个环节会怎么挂」：信源冒充、粒度拿不到、预算耗尽、口径滑动、沉默跳过……每条失败模式反向补一条设计决策。全项目积累失败模式数十条，全部能追溯到具体事故编号。
**案例**：口径冒充（单一机构数字冠以「全国」）→ 设计 3\.1 口径层级行；补搜耗尽才暴露粒度边界 → 设计粒度信号 \+ 粒度缺口交付规格。

### 2\. 约束大于判断

FM 的软判断会漂移（「差不多得了」「措辞相近」），规则必须把关键决策从「自由心证」变成「机械检查」。
**案例**：补搜停止从「FM 觉得够了」→ 双判断（缺口闭合核对表 \+ 期望收益枚举）；口径等价从「措辞相近即可」→ 默认拒绝 \+ 逐维文本比对 \+ 举证义务。

### 3\. 可审计交付

交付物必须能回答「这个数字哪来的、为什么停在这、试过哪些路」。停止无记录 = 违规；URL 缺失 = 证据不存在；转述 = 污染。
**案例**：v2\.5 起停止必须附阻断事实 \+ URL，无证据停止即违规（8/5 实测暴露「搜满轮次才宣布找不到」的坏行为）；「不适用」必须附深评记录（v2\.4 补丁）。

### 4\. 修复存在 ≠ 修复验证

补丁落盘只是开始，必须在原始失败现场复跑验证，再由外部核销闭环。每一步留核销痕迹。
**案例**：v2\.5\.1 场景 12 实测失败（等价判断失守）→ v2\.5\.2 四件套修复 → 12\-R 同题复跑全判据通过。同查询、同诱饵、判定方向反转——这是「修复被验证」的完整范例。

---

## 二、搜索流程设计：先建白名单，再进搜索

### 2\.1 两阶段分离（Phase 1 领域分析 → Phase 2 检索）

**设计**：第一阶段不检索答案，只做四件事——结构判定（树状/网状/单源垄断）、候选信源发现、逐个深评、构建白名单 \+ 储备清单；第二阶段才在白名单内检索数据。
**为什么**：检索质量的上限由信源池决定。先评估信源再检索，垃圾源进不了数据池；先检索再筛，垃圾数据已经污染了判断。
（两阶段分离是初版设计推演即定的原则，此后全部场景都是它的验证——如 9\-R′ 从零建档时白名单先于检索建立）
**核心表述**：白名单是**产出物**，不是输入物。

### 2\.2 深评三关

候选信源必须过：身份确认（打开页面、核实机构）→ 数据溯源（数字是否带出处）→ 数据状态（版本/年份/是否修订过历史值）。任一关不过 → 淘汰或进储备清单。
**案例**：v2\.3 N=3 复跑发现「不适用」过早声明——未经深评就预设终态；补丁后「不适用」必须附深评记录。

### 2\.3 信源发现的两个技巧

① **搜引用不搜推荐**——权威源往往不自带关键词（年报正文提数据但标题不含指标名），搜「谁引用了这个数字」比搜「这个数字是什么」更可靠；
② **英文辅助搜索**——非英语领域的官方数据常以英文版发布（UN / IMF / IEA 等国际机构），中英双语检索互相印证，英文关键词常能命中官方英文页。

### 2\.4 达标即停、下限封槽

每个数据槽位达到证据下限（≥2 个独立一手源）即停，不满则标注空缺；不无限搜集。
**配套**：储备清单（惰性求值）——候选源先入清单不深挖，槽位需要补位时才取一篇深评，补位必先深评。
**为什么**：成本与领域复杂度脱钩——槽位可数，达标即停。

### 2\.5 沉默即违规

每个槽位必须有终态：达标填上，或明确标空缺 \+ 原因。不许「没找到但没说」。
**案例**：v2\.1 场景 2——归因槽位被静默跳过，FM 填了「第三方观点 ×2」就停；修复：封槽规则拆行 \+ 「沉默跳过即违规」。

---

## 三、补搜预算设计：从「轮次上限」到「证据决定停止」

### 3\.1 演进路线

- **v2\.1\-v2\.4**：固定轮次上限。问题：够用时就该停，不够用时轮次不够；「搜满轮次才宣布找不到」成为常见坏行为

- **v2\.5**：双判断——每轮补搜后问两个问题：① 信息缺口闭合了吗？（对照自查清单逐子问题核对）② 继续搜还有期望收益吗？（未试动作枚举：白名单剩余信源、未试关键词、储备候选）。停止必须附证据——正阻断事实 \+ URL（如付费墙原文，一证两用兼作付费路径证据），无证据停止 = 违规。2 轮上限降级为**安全网**而非驱动

- **配套：截断可见性**——停止时交付「未试动作清单」，用户可授权追加轮次；cap 截断不沉默

### 3\.2 为什么砍掉「可得性预检」

曾设计前置预检（先判断目标粒度是否公开可得再决定轮次），实测后砍掉：预检在信息最差时点做判断，假阴性风险不对称（误判「拿不到」会杀死可检索任务）；双判断②本身就是可得性判断——搜一轮发现该粒度公开没有，期望收益归零即止损。**同一个判断，放在信息更多的时候做**。

### 3\.3 核心洞见

**停止条件 = 证据决定，不是预算决定。** 预算只是保险丝。这个反转让「提前停」（信息够就停）和「止损停」（期望收益为零）都有了合法出口。

---

## 四、口径纪律：数据检索最容易被忽略的坑

### 4\.1 口径层级匹配（防外部冒充）

声称的层级必须与出处主体层级匹配：单一机构数字不得自称全国、省级不得冒充全国。
**案例**：chyxx/华经转引单一认证机构「全国 18\.15 万亩」——云南一省官方 81\.8 万亩即证伪。机械检查：声称层级 vs 主体层级。

### 4\.2 交付侧口径滑动（防自己犯错）

外部源防住了，自己交付时同样会滑：白名单内官方数据的口径范畴没锁死。
**案例（9\-R 事故）**：云南 2025 年「有机**产品**认证面积 190\.02 万亩」（全省所有作物）被当作「有机**茶园**认证面积」（茶类细分）的全国下限使用，还写「全国 ≥ 190\.02 万亩」——产品 ⊃ 茶园，190\.02 是茶园面积的**上界**，不能当下界。且「口径一致递增序列」假象（81\.8→189\.3→190\.02）是口径切换制造的。

### 4\.3 三道防线（v2\.5\.1）

- **关键口径三要素**：档案必须记录查询口径的层级 × 范畴 × 边界（与覆盖范围字段同族不同物：覆盖范围=信源侧「源有什么」，关键口径=查询侧「用户要什么」）

- **口径连续性检查**：新数据点进序列/替换边界值前必须核对口径一致；不一致 → 不得替换、不得进同一序列，单独成列 \+ 显式标注

- **不等式禁令**：异口径数字禁止进入 ≥/≤ 推断，只能作异口径参照单独标注

### 4\.4 等价判定（v2\.5\.2，默认拒绝）

实测发现三道防线仍有一环失守：**等价判断凭自由心证**——「措辞相近但非逐字相同」被当成「同口径」放行（场景 12：「识别了、标注了、照用了」；且量级衔接 \+0\.38% 反被当成同口径证据）。
**修复**：口径等价判定单点定义——三要素逐维文本比对，任一维标签不同 → **默认异口径**；主张等价必须举证（引双方定义原文、逐维论证、记入档案）。三个「不是证据」：措辞相近不是证据、量级衔接不是证据、与其他序列自洽不是证据。**包含关系 ≠ 等价**（超集数字只能作参照，不进序列/不作界/不进不等式）。
**还有**：「显式」合法的仅限并存记录（单独成列 \+ 标注）；承担查询口径角色永远要求等价判定通过——**标注差异不构成使用许可**。

### 4\.5 粒度信号与粒度缺口交付规格

用户要的粒度公开不存在（榜单/明细/汇总不可得）时：交付公开可得最高粒度 \+ 缺口如实标注 \+ 尝试清单 \+ 实证遇见的付费路径。禁止编造补齐。**「部分交付 \+ 诚实标注」永远好过「死磕完整 \+ 最后才说拿不到」。**

---

## 五、证据链与可信度

- **来源卡片**：每个数字带 URL \+ 逐字原文引用（关键数字原文引用，转述不算）

- **交叉验证**：跨源一致性检查；差异分三类处理——年份差异、定义边界差异（不裁决、不换算）、口径冒充（证伪淘汰）

- **独立性分级**（L1\-L3）：同源转引不重复计数；「数据生产者 / 官方确认 / 国际汇编」的角色区分

- **第三方鉴证双类**：财务审计 / 方法论认证，两类不可互相冒充（v2\.4 起定义）；降级信源可填槽但带 ⚠️

- **工具限制 vs 信源不可信**：反爬/JS 防护/域名迁移 → 记工具可访问性限制，不误判信源（curl 403 换 WebFetch 即证明）

---

## 六、档案与跨会话复用

- **领域档案**：白名单、槽位终态、淘汰记录、冲突裁决、关键口径、保鲜期——是长期资产不是一次性产物

- **保鲜分级**：保鲜期内直接复用（不重跑 Phase 1），超期全项复核，复核结果回填；未声明默认 90 天

- **复用纪律**：旧版档案缺新字段 → 顺手补齐写回（不触发大复核）；档案更新必须过口径连续性检查

- **跨会话复用的正确姿势**：Step 0 双查（预置 Skill \+ 工作区档案）是强制启动步骤——没有它，复用靠 FM 自发发现

---

## 七、测试与迭代方法论（怎么证明 skill 真的变强了）

### 7\.1 判据写行为，不写数字

断言是行为断言（「停止附证据且 URL 在实录中出现」），数值以实测为准——防测试变成数字表演。

### 7\.2 执行者不知道判据

判据是核销人的尺子；执行者知道判据会「表演」（知道要 URL 就刻意留 URL）。只给 SKILL 全文 \+ 查询 \+ 产出要求。

### 7\.3 隔离清单

每场景执行前：清空工作区同领域档案、夹具头部注明、全新会话、无前轮记录——逐条打勾，漏一条都是效度缺陷。

### 7\.4 剧透效度声明

SKILL 正文自带实例（剧透）→ 复跑效度限于「机制形态」，不证明「独立发现」；独立发现能力由无剧透新场景验证。归因必须区分「剧透驱动的正确 vs 机制驱动的正确」。
**案例**：v2\.5\.1 场景 12 的教训——例句剧透不能替代机制（「会背没会用」）。

### 7\.5 对照组防漂移

单测失败场景可能把机制推向另一个极端（过度降级/躺平）——公开可得对照场景双向钳制。

### 7\.6 外部核销闭环

第三方（Kimi）逐条核销：判据核销 \+ 关键证据抽查（URL 逐个 200、数字比对原文）\+ 残留 grep。**外部视角抓到过我们两次大错**：9\-R 口径滑动（Kimi 核销发现）、11B「13 港付费」数据误判（WorkBuddy 交叉测试发现）。

### 7\.7 修复存在 ≠ 修复验证（循环）

补丁落盘 → 核销（文本在）→ 原始失败现场复跑（行为对）→ 外部核销（账对）→ 文档同步。每步留痕。
**Before/After 金案例**：场景 12「识别了没拦住」→ v2\.5\.2 四件套 → 12\-Ra 同题反转「判对且拦住」。

### 7\.8 测试环境隔离（Hermes profile 方案）

测试用独立 profile：独立 skills 目录（只放被测 skill 一份）\+ 空记忆 \+ 独立会话/数据库 \+ SHA 版本锁。真 skill 机制加载（不是塞全文 prompt），版本天然锁死，并行会话改生产目录碰不到测试副本。测试前后各算一次指纹，漂移即作废。

---

## 八、工程协作机制

- **外部评议闭环**：材料自包含（背景 30 秒版 \+ 逐问题现象/结论/请评议点）→ 第三方独立判断 → Hermes 逐条独立判断（不默认全盘接受）→ PAPER 终审 → 落地

- **走查文档**：改进点先记录（现象→问题→方向，带事故编号），拍板前不落地进 skill；设计原则也记入

- **单点定义 \+ 规则地图**：每个概念一个唯一定义点，其余只引用；规则族级梳理（口径规则地图 10\+ 概念），防族内漂移

- **双版同步**：执行版（实战进化）\+ 公开版（repo 门面），规则必须双版对齐；patch 前先 diff 现状（并行 session 可能已改）

- **失败模式倒推表**：设计决策全部可追溯——每条规则回答「当时哪个事故让我这么改」

---

## 附：迭代历程速览

|版本|关键机制|驱动事故|
|---|---|---|
|v2\.1|六步领域分析 \+ 标准检索|初版设计推演|
|v2\.2|封槽规则拆行 \+ 沉默跳过即违规|场景 2 翻车|
|v2\.3|深评证据约束 \+ 档案保鲜|两篇外部评议 \+ N=3 复跑|
|v2\.4|第三方鉴证双类 \+ 档案保鲜分级|N=3 暴露三问题|
|v2\.4\.2|第零步双查|场景 7 档案跨会话复用|
|v2\.5|双判断 \+ 口径层级三点链 \+ 粒度缺口规格 \+ 粒度信号|走查 \#6\-\#9（8/5 两次实测）|
|v2\.5\.1|关键口径三要素 \+ 连续性检查 \+ 不等式禁令|9\-R 交付侧口径滑动（\#11）|
|v2\.5\.2|口径等价判定（默认拒绝）\+ 显式≠可用 \+ 量级陷阱条|场景 12 等价判断失守|

**一句话收尾**：行业定义 agent 的本质不是「会检索」，而是「知道什么时候该信、什么时候该停、怎么证明自己是对的」——这三件事每件都是用事故换来的。

