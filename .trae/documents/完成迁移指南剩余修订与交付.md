# 完成迁移指南剩余修订与交付

> 计划日期：2026-07-01
> 目标文件：`/Users/paper/trae_project/行业定义agent/agent团队经验迁移指南-YOLOpose科研项目.md`（当前 977 行）
> 背景：上一轮会话已完成 fact-checker 核查 + architecture-critic 审查，并应用了大部分 P0/P1 修正。本计划完成剩余 6 项 P1/P2 修正 + 版本脚注更新 + 交付汇报。

---

## 当前状态分析

文件已通过 ai-architecture-fact-checker 核查（18 项准确，2 项已修正，3 项部分准确已标注），并已应用 architecture-critic 的大部分 P0/P1 修正（SOLO Agent 前置条件、Step 4 硬前置、5.5 工程视角、5.6 GPU 约束、PostToolUse→PreToolUse 备份、agent 边界列）。

**剩余未应用的修正**（经 Phase 1 探索确认）：

| 编号 | 行号 | 问题 | 修正 |
|------|------|------|------|
| P1-4 | 487-489 | 4.3 节"创建方式"仍含文件系统分支，与 7.1 已结论矛盾 | 删除文件系统分支，统一为 IDE 创建 |
| P1-5 | 61,62,67,68 | 规则 5/6/11/12 写"直接迁移，无需修改"过度乐观 | 改为"直接迁移，需验证适配性" |
| P1-7 | 94 | "训练 1 个 epoch 不报错"标准过弱 | 增加"且 loss 呈下降趋势" |
| P2-9 | 82 | "解决 agent 发现性不足"夸大效果 | 改为"缓解" |
| P2-10 | 668 | "CVPR/ICCV 格式"硬编码会议名 | 改为"目标会议格式"（line 299 的 arXiv/CVPR/ICCV 是举例 venue，保留不动） |
| P2-11 | 918+ | 附录未记录 fact-checker 具体修正项 | 新增 8.6 节列出 2 项修正 + 3 项部分准确 |

---

## 实施步骤

### 步骤 1：应用 P1-4（4.3 节创建方式分支）

**文件**：`agent团队经验迁移指南-YOLOpose科研项目.md`
**位置**：lines 487-489
**操作**：Edit

将：
```
> **创建方式**（取决于第 7 章核查结果）：
> - 若 Trae 支持 `.trae/agents/` 文件系统定义：将以下提示词写入 `.trae/agents/<name>.md`（含 YAML frontmatter）
> - 若仅支持 IDE 界面创建：在 IDE 中"创建智能体"，将以下提示词填入"提示词"字段，开启"可被其他智能体调用"，配置英文标识名和调用时机
```

改为：
```
> **创建方式**（已由 fact-checker 核查确认，见 7.1 节结论）：
> Trae 智能体**仅支持 IDE 界面创建**，不支持 `.trae/agents/` 文件系统定义。在 IDE 对话输入框输入 `@` → 点击"创建智能体" → 将以下提示词填入"提示词"字段 → 开启"可被其他智能体调用" → 配置英文标识名和调用时机。
> ⚠️ 以下 YAML frontmatter 仅为提示词内容组织参考，**不是文件系统定义**——IDE 创建时将 frontmatter 下方正文填入"提示词"字段即可。
```

### 步骤 2：应用 P1-5（4 处"直接迁移，无需修改"）

**位置**：lines 61, 62, 67, 68
**操作**：Edit（replace_all 不适用，因每行上下文不同，需逐处或用唯一上下文）

- line 61: `| 5 代码建议必须具体 | 文件名+伪代码+工作量+优先级 | 直接迁移，无需修改 |` → `... | 直接迁移，需验证适配性 |`
- line 62: `| 6 文件修改前必须先读 | 修改已有文件先 Read 完整读取 | 直接迁移，无需修改 |` → `... | 直接迁移，需验证适配性 |`
- line 67: `| 11 采纳建议前去偏见化 | 验证技术前提+说明选择理由+记录反驳 | 直接迁移，无需修改（科研场景同样适用） |` → `... | 直接迁移，需验证适配性（科研场景同样适用） |`
- line 68: `| 12 多 agent 意见冲突仲裁 | 代码事实>已确认根因>trade-off 表>保守 | 直接迁移，无需修改 |` → `... | 直接迁移，需验证适配性 |`

### 步骤 3：应用 P1-7（"实现完成"定义补强）

**位置**：line 94
**操作**：Edit

将：
```
**迁移要点**：YOLOpose 场景的"实现完成"应包含——模型代码可运行（能 forward）+ 单元测试通过（输出形状正确）+ 训练 1 个 epoch 不报错 + 权重可保存加载。
```
改为：
```
**迁移要点**：YOLOpose 场景的"实现完成"应包含——模型代码可运行（能 forward）+ 单元测试通过（输出形状正确）+ 训练 1 个 epoch 不报错且 loss 呈下降趋势 + 权重可保存加载。
```

### 步骤 4：应用 P2-9（"解决"→"缓解"）

**位置**：line 82
**操作**：Edit

将 `解决 agent 发现性不足问题` 改为 `缓解 agent 发现性不足问题`。

### 步骤 5：应用 P2-10（会议格式泛化）

**位置**：line 668
**操作**：Edit

将 `3. 撰写章节内容（遵循 CVPR/ICCV 格式）` 改为 `3. 撰写章节内容（遵循目标会议格式，如 CVPR/ICCV/ECCV 等）`。

### 步骤 6：应用 P2-11（附录新增 8.6 fact-checker 核查记录）

**位置**：line 960 之后（8.5 节表格之后、`---` 分隔线之前）
**操作**：Edit，新增 8.6 节

内容：
```markdown
### 8.6 fact-checker 核查记录（2026-07-01）

> 以下为 ai-architecture-fact-checker 对第 3 章和第 7 章的核查结果，供新 agent 了解哪些数据已交叉验证。

**2 项已修正错误**：
1. Skills 目录结构：`references/`→`resources/`、`assets/`→`templates/`（官方文档为准）
2. Skills URL：`docs.trae.ai/ide/skills`→`docs.trae.cn/ide/skills`（国内版地址）

**3 项部分准确（已标注待验证）**：
1. Skills 三层加载 Token 数（"约 100 Token"、"<5000 Token"）：官方文档未给出具体数值，已标 ★☆☆☆☆ 待验证
2. `tool_input.file_path` 字段路径：基于 Trae 与 Claude Code Hook 兼容性推断，官方文档未显式列出 Edit/Write 的 tool_input 内部字段，待 IDE 实测
3. exit 0 + 无 stdout 等同 allow：从 schema 推断，官方文档未显式说明，待 IDE 实测
```

### 步骤 7：更新版本脚注

**位置**：line 977
**操作**：Edit

将：
```
*文档版本：v1.1 | 日期：2026-07-01 | 核查状态：已通过 ai-architecture-fact-checker 核查（18 项准确，2 项已修正，3 项部分准确已标注），待 architecture-critic 审查*
```
改为：
```
*文档版本：v1.2 | 日期：2026-07-01 | 核查状态：已通过 ai-architecture-fact-checker 核查（18 项准确，2 项已修正，3 项部分准确已标注）+ architecture-critic 审查（P0×3 + P1×8 + P2×11 已全部处理）*
```

### 步骤 8：验证全部修正已应用

**操作**：Grep 验证
- 确认 `直接迁移，无需修改` 不再出现（应为 0 命中）
- 确认 `取决于第 7 章核查结果` 不再出现
- 确认 `训练 1 个 epoch 不报错且 loss 呈下降趋势` 存在
- 确认 `缓解 agent 发现性不足` 存在
- 确认 `目标会议格式` 存在
- 确认 `8.6 fact-checker 核查记录` 存在

### 步骤 9：交付汇报

向用户汇报：
- 文件路径
- fact-checker 核查摘要（2 修正 + 3 部分准确）
- architecture-critic 审查摘要（P0/P1/P2 处理情况）
- 已知待验证项（第 7 章 5 项，需新 agent 在 YOLOpose 项目中实测）

---

## 假设与决策

1. **不重新调用 architecture-critic**：剩余 6 项修正均是对已审查反馈的逐条应用，不引入新内容，无需二次审查。
2. **P2-10 仅改 line 668**：line 299 的 `arXiv/CVPR/ICCV` 是在可信度表格中举例论文 venue（非格式规范），保留不动。
3. **Write/Edit 工具磁盘 bug**：若 Edit 直接写入失败，回退方案为 Write 到 `/tmp/` + `Shell cp`（project_memory 记录的已知 workaround）。

## 验证

- 步骤 8 的 Grep 验证全部通过
- 文件总行数应在 985-995 之间（新增 8.6 节约 15 行）
- 版本脚注为 v1.2
